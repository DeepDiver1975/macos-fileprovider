* *2026-08-16 (on a Mac)*: Added `OwnCloudCore.RemoteClient` — the transport seam every Phase 4 operation builds on — test-first (127 tests green). It turns a `RemoteRequest` into an HTTP round-trip via `URLRequestFactory`, attaches `Authorization`, and translates the response: 2xx → body bytes, any other status → the classified `RemoteError` thrown; a transport-level failure propagates unchanged. The network call is an injected `async` performer, so request-shaping + response-classification are tested with a stub (no live server) and the type stays Linux-buildable; a `URLSession`-backed `.urlSession(_:)` performer is provided for production. 5 tests (success body, auth header, 404→noSuchItem, 412→versionConflict, transport-error passthrough). This gives the replicated-extension handlers a single tested chokepoint for issuing the Phase 4 `RemoteRequest`s and mapping failures; the remaining Mac-only glue is byte streaming to/from the FileProvider temp-URL hand-off.
* *2026-08-17 (on a Mac)*: **Task 4.4 push request shaping + `deleteItem` wired (191 core tests green, 2 skipped).** Test-first, `BackendConnection` now vends the per-backend push requests the handlers route — Classic (path-addressed): `createFileRequest`/`createDirectoryRequest`/`modifyContentsRequest(path:ifMatchETag:)`/`deleteRequest(path:)`/`moveRequest(fromPath:toPath:)`; oCIS (ID-addressed): `modifyContentsRequest(itemID:ifMatchETag:)`/`deleteRequest(itemID:)`/`createFolderRequest(parentID:name:)` — 10 tests over the existing `WebDAVRequestBuilder`/`GraphRequestBuilder`. `FileProviderExtension.deleteItem` is now wired end-to-end (the one push handler needing no metadata read-back): resolve the item's backend address from its identifier, issue the DELETE over the shared `RemoteClient`, report `nil` / a mapped `NSFileProviderError`. `createItem`/`modifyItem` still return `NSFeatureUnsupportedError` — they must return a reconciled `NSFileProviderItem` (server-assigned id + version), which needs the metadata cache. `xcodegen generate` + signed `xcodebuild -scheme App -allowProvisioningUpdates build` → **BUILD SUCCEEDED**.
